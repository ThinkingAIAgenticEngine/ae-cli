# +batch_delete_experiment

Batch delete experiments.

```bash
ae-cli experiment +batch_delete_experiment --project_id <id> --exp_ids '["exp_1","exp_2"]' [--yes]
```

Flags:
- `--project_id`, `-p`: Project ID.
- `--exp_ids`: Experiment ID list as JSON array.
