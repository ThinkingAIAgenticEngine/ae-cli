# +delete_metric

Delete a metric.

```bash
ae-cli experiment +delete_metric --project_id <id> --metric_id <metricId> [--yes]
```

Flags:
- `--project_id`, `-p`: Project ID.
- `--metric_id`: Metric ID.
