# +batch_delete_feature

Batch delete Features.

```bash
ae-cli experiment +batch_delete_feature --project_id <id> --req '<json>' [--yes]
```

Flags:
- `--project_id`, `-p`: Project ID.
- `--req`: Feature delete request JSON object.
