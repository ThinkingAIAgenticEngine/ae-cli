# +query_experiment_detail

Query full experiment detail.

```bash
ae-cli experiment +query_experiment_detail --project_id <id> --exp_id <expId>
```

Flags:
- `--project_id`, `-p`: Project ID.
- `--exp_id`: Experiment ID.
