# +manage_feature_status

Update Feature status.

```bash
ae-cli experiment +manage_feature_status --project_id <id> --req '<json>'
```

Flags:
- `--project_id`, `-p`: Project ID.
- `--req`: Feature status request JSON object.
