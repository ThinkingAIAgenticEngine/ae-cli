# +batch_delete_traffic_layer

Batch delete traffic layers.

```bash
ae-cli experiment +batch_delete_traffic_layer --project_id <id> --layer_ids '["layer_1","layer_2"]' [--yes]
```

Flags:
- `--project_id`, `-p`: Project ID.
- `--layer_ids`: Traffic layer ID list as JSON array.
