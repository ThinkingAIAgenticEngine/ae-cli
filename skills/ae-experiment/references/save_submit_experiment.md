# +save_submit_experiment

Create or update a complete experiment and submit it in one call.

```bash
ae-cli experiment +save_submit_experiment --project_id <id> --req '<json>'
```

Flags:
- `--project_id`, `-p`: Project ID.
- `--req`: Save-and-submit request JSON object.

Prefer `+save_experiment` for progressive draft construction.
