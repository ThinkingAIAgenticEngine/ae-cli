# +query_metric_detail

Query metric detail.

```bash
ae-cli experiment +query_metric_detail --project_id <id> --metric_id <metricId>
```

Flags:
- `--project_id`, `-p`: Project ID.
- `--metric_id`: Metric ID.
