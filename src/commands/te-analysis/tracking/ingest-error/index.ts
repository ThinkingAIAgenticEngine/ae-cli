import type { Command } from '../../../../framework/types.js';
import { trackingIngestErrorList } from './list.js';

const commands: Command[] = [
  trackingIngestErrorList,
];

export default commands;
export { trackingIngestErrorList };
