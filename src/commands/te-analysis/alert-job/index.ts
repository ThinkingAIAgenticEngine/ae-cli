import type { Command } from '../../../framework/types.js';
import { analysisAlertJobList } from './list.js';

const commands: Command[] = [
  analysisAlertJobList,
];

export default commands;
export { analysisAlertJobList };
