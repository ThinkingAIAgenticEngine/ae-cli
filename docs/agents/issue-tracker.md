# Issue tracker

This feature uses branch-local Markdown under `.scratch/ae-analysis-fix/`: `spec.md` and one ticket per `issues/` file. Dependency edges are recorded as ticket numbers. Statuses are ready-for-agent, in-progress, complete and blocked. No remote issue publication. The user authorized this local branch workflow.
